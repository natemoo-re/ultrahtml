{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @example/with-snarkdown run dev",
    "build": "pnpm --filter ultrahtml run build"
  }
}
